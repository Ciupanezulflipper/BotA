name: Package 7 complexity materialize

on:
  push:
    branches:
      - fix/package7-control-plane-stability-20260817
    paths:
      - .github/workflows/package7-materialize-complexity.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Materialize complexity cleanup
        run: python ops/package7_materialize_complexity.py

      - name: Compile changed Python
        run: |
          python -m py_compile \
            tools/native_service_daemon_watchdog.py \
            tests/test_native_service_daemon_watchdog_package7.py

      - name: Run focused control-plane tests
        run: |
          python -m unittest -v \
            tests/test_native_service_daemon_watchdog.py \
            tests/test_native_service_daemon_watchdog_package7.py \
            tests/test_native_service_daemon_migration.py

      - name: Diff hygiene
        run: git diff --check

      - name: Commit cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/package7-materialize-complexity.yml ops/package7_materialize_complexity.py
          git add tools/native_service_daemon_watchdog.py tests/test_native_service_daemon_watchdog_package7.py
          git diff --cached --check
          git commit -m "refactor: simplify manager-loss drain helpers"
          git push origin HEAD:fix/package7-control-plane-stability-20260817
