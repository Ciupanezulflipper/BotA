name: Package 7 materialize

on:
  push:
    branches:
      - fix/package7-control-plane-stability-20260817
    paths:
      - .github/workflows/package7-materialize.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Materialize bounded manager-loss fix
        run: python ops/package7_materialize.py

      - name: Compile changed Python
        run: |
          python -m py_compile \
            tools/native_service_daemon_watchdog.py \
            tests/test_native_service_daemon_watchdog_package7.py

      - name: Run control-plane focused tests
        run: |
          python -m unittest -v \
            tests/test_native_service_daemon_watchdog.py \
            tests/test_native_service_daemon_watchdog_package7.py \
            tests/test_native_service_daemon_migration.py

      - name: Diff hygiene
        run: git diff --check

      - name: Commit materialized runtime fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/package7-materialize.yml ops/package7_materialize.py
          git add tools/native_service_daemon_watchdog.py tests/test_native_service_daemon_watchdog_package7.py
          git diff --cached --check
          git commit -m "fix: converge manager-loss recovery before native restart"
          git push origin HEAD:fix/package7-control-plane-stability-20260817
